// This file is the holding tank for all of the game's dialog. Ties into
// other files within the data/sequences/* files. (TODO)
{
seqData: "{string -> [commands]} -> {
	'null': [],
	'hi': [debug('hi')],
	'go_left': [map(range(1000), fire_event('input_l') )],
	'_go_right': [map(range(1000), fire_event('input_r') )]
}" // end of seqData

} // end of file
