// dialog_nodes.cfg == contains references to all of the game's dialog.
// For now, the dialog is going right in here, but later on it will be
// split into multiple files using "@include _dialog/part_a.cfg", etc.
// These should probably not be put together in one large list, as
// dialog_nodes is a map.

{
	intro: {
		on_entry: "
		def(obj mage_playable player,
			obj dialog_display dialog) -> commands [
				add(dialog.text, q~Greetings, explorer!~)
		]
}
